import 'package:flutter/material.dart';

class IconColors {
  static const Color menuColor = Colors.white;
  static const Color searchColor = Colors.white;
}

const iconColor = IconColors.menuColor;
const searchColor = IconColors.searchColor;
